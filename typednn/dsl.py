# implementation of basic operators
#   include contact, batchify, type conversion
#   we implement abstract methods, for which one can either (use the default config, parse the config to build a new one, or use a shared config)
